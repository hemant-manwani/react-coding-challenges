const DARK_DISPLAY_MODE = 'dark';
const LIGHT_DISPLAY_MODE = 'light';

const display = {
  DARK_DISPLAY_MODE,
  DARK_MODE_CLASSNAME: 'dark-mode',
  DEFAULT_DISPLAY_MODE: LIGHT_DISPLAY_MODE,
  LIGHT_DISPLAY_MODE,
};

export default display;